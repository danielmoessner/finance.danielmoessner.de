# PUTTY
apt-get upgrade
apt-get update
apt-get install python3-pip apache2 libapache2-mod-wsgi-py3
pip3 install virtualenv
cd /home
mkdir xyz_project
cd xyz_project
mkdir xyz
cd xyz
mkdir xyz
cd

# WINSCP
--upload project files

# PUTTY
cd /home/xyz_project
virtualenv xyzenv
source xyzenv/bin/activate
cd xyz/requirements/
pip install -r base.txt
cd ..
python manage.py runserver 0.0.0.0:8000  
--see if everything works except the static files
CONTROL-C
deactivate
cd
nano /etc/apache2/sites-available/000-default.conf

# EDIT THE FILE EXAMPLE FILE
Listen 8012
<VirtualHost *:8012>
        ServerAdmin webmaster@localhost
        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # farming_project
        Alias /static /home/farming_project/farming/farming/static
        <Directory /home/farming_project/farming/farming/static>
                Require all granted
        </Directory>

        Alias /media /home/farming_project/farming/farming/media
        <Directory /home/farming_project/farming/farming/media>
                Require all granted
        </Directory>

        <Directory /home/farming_project/farming/config>
                <Files wsgi.py>
                        Require all granted
                </Files>
        </Directory>

	WSGIDaemonProcess farmingprocess python-path=/home/farming_project/farming python-home=/home/farming_project/farmingenv
        WSGIProcessGroup farmingprocess
        WSGIScriptAlias / /home/farming_project/farming/config/wsgi.py
        # end farming_project
</VirtualHost>

# PUTTY
cd /home/xyz_project
chmod 777 xyz
chmod 666 xyz/db.sqlite3
find xyz/logs/ -type f -exec chmod 666 {} \;
service apache2 restart
